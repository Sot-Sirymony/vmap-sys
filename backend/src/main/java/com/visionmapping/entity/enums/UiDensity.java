package com.visionmapping.entity.enums;

/**
 * FR-18.4 / FR-39: how tightly rows and cards are packed. Compact narrows the
 * paddings that decide how many records fit on screen; it is a spacing choice
 * only and never hides information.
 */
public enum UiDensity {
    COMFORTABLE,
    COMPACT
}
