package com.visionmapping.entity.enums;

/**
 * FR-50.4: what drives the partner, as distinct from {@link OfferType} —
 * which records what the user offers in return.
 */
public enum PartnerMotivator {
    FINANCIAL_GAIN,
    AVOIDING_LOSS,
    SHARED_VISION,
    RECOGNITION,
    OTHER
}
