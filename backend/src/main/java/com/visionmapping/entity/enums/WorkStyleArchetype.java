package com.visionmapping.entity.enums;

/**
 * FR-49: a two-axis (pace x focus) work-style archetype. Original labels —
 * not the source model's own named framework — for a Driver/Connector/
 * Steadier/Planner quadrant: DRIVER (fast-deciding + task-oriented),
 * CONNECTOR (fast-deciding + people-oriented), STEADIER (deliberate +
 * people-oriented), PLANNER (deliberate + task-oriented).
 */
public enum WorkStyleArchetype {
    DRIVER,
    CONNECTOR,
    STEADIER,
    PLANNER
}
